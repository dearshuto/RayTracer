[package]
name = "server_http"
version = "0.1.0"
edition = "2021"

[dependencies]
sjrt-net = { path = "../sjrt-net" }

clap = { version = "3", features = ["derive"] }
tokio = { version = "1", features = ["full"] }
